apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ds2identityhub.app.name }} 
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.ds2identityhub.app.name }} 
spec:
  replicas: {{ .Values.ds2identityhub.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.ds2identityhub.app.name }} 
  template:
    metadata:
      labels:
        app: {{ .Values.ds2identityhub.app.name }} 
    spec:
{{- if .Values.ds2identityhub.serviceAccount.create }}
      serviceAccountName: {{ .Values.ds2identityhub.app.name }}-serviceaccount
{{- end }}
      containers:
      - name: {{ .Values.ds2identityhub.app.name }}
        image: {{ template "ds2identityhubimageURL" . }}
        imagePullPolicy: {{ .Values.ds2identityhub.image.pullPolicy }} 
        ports:
        - name: http 
          containerPort: 8080
        {{- if .Values.resourcesEnabled }}
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        {{- end }} 
{{- if .Values.ds2identityhub.app.env }}
        envFrom:
        - configMapRef:
            name: {{ .Values.ds2identityhub.app.name }}-configmap
{{- end }}
{{- if .Values.ds2identityhub.app.configfile }}
        volumeMounts:
        - name: {{ .Values.ds2identityhub.app.name }}-configfilevolume
          mountPath: /docker-entrypoint-initdb.d/init.sql
          subPath: init.sql
      volumes:
        - name: {{ .Values.ds2identityhub.app.name }}-configfilevolume
          configMap: 
            name: {{ .Values.ds2identityhub.app.name }}-configfilemap
{{- end }}
      dnsConfig:
        options:
          - name: ndots
            value: "1"
{{- if .Values.ds2identityhub.image.privateRegistry.enabled }}
      imagePullSecrets:
      - name: {{ .Values.ds2identityhub.app.name }}-imagepullsecret
{{- end }}

