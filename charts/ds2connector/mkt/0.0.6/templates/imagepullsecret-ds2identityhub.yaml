{{- if and (.Values.ds2identityhub.image.privateRegistry.enabled) (not .Values.ds2identityhub.image.privateRegistry.global) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ds2identityhub.app.name }}-imagepullsecret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "secret" . }}
{{- end }}

