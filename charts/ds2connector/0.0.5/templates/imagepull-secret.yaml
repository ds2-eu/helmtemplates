{{- if and (.Values.postgres.image.privateRegistry.enabled) (not .Values.postgres.image.privateRegistry.global) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgres.app.name }}-imagepullsecret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "secret" . }}
{{- end }}

