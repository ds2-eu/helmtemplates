{{- if and (.Values.ds2dataplane.image.privateRegistry.enabled) (not .Values.ds2dataplane.image.privateRegistry.global) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ds2dataplane.app.name }}-imagepullsecret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "secret" . }}
{{- end }}

