{{- if .Values.ds2controlplane.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.ds2controlplane.app.name }}-serviceaccount
  namespace: {{ .Release.Namespace }}
{{- end }}
