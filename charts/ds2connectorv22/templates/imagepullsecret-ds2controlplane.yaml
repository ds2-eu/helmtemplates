{{- if and (.Values.ds2controlplane.image.privateRegistry.enabled) (not .Values.ds2controlplane.image.privateRegistry.global) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ds2controlplane.app.name }}-imagepullsecret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "secret" . }}
{{- end }}

